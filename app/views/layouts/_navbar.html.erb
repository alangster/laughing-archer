<div>
	<%= link_to("ALL BLOGS", :root) %>
	<% if !current_user %>
	<%= link_to("SIGN UP", new_user_path) %>
	<%= link_to("SIGN IN", new_session_path) %>
	<% else %>
	<%= link_to("PROFILE", current_user) %>
	<%= link_to("WRITE A NEW POST", new_post_path) %>
	<%= link_to("SIGN OUT", '/signout') %>
	<% end %>
</div>