<h1>Shiny New Blogs!</h1>
<table>
<% @posts.each do |post| %>
	<tr>
		<td><%= link_to "#{post.title}", post %></td>
		<td><%= link_to "#{post.author.name}", post.author %></td>
	</tr>	
<% end %>	
</table>
